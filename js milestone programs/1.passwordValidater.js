let password;
let confirmPassword;

// password=prompt("Enter password");
// confirmPassword=prompt("Enter confirm password");
password="pass1"
confirmPassword="pass1"

if (password===confirmPassword) {
    console.log("password matches")
} else {
    console.log("password not matches")
}
